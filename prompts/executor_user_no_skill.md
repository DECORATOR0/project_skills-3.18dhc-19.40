Task payload:
{task_json}

Activated skill name:
(none; this baseline intentionally does not provide a routed skill)

Activated SKILL.md:
(not provided)

Bundled resources visible to you:
[]

Execution reminder:
- there is no skill bundle in this baseline
- use only the visible tool list shown in the system prompt and choose tools step by step inside that list
- if a tool needs an input file path, expand `file_list_preview` entries to full paths under `data_dir` unless you already have a tool-returned absolute path
- do not assume hidden references, scripts, or skill-specific constraints that were not explicitly provided

Execute the task now. Infer the tool path yourself.
